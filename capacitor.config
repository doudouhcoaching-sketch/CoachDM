import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.coachdm.app',
  appName: 'Coach DM',
  webDir: 'www',

  server: {
    /* CLE DU CORRECTIF YOUTUBE
       Par defaut Capacitor sert en capacitor://localhost. YouTube refuse tout
       embed dont l'origine n'est pas http(s) et affiche "Regarder sur YouTube".
       En https, l'origine devient https://localhost et le lecteur fonctionne. */
    iosScheme: 'https',
    androidScheme: 'https'
  },

  ios: {
    contentInset: 'never',
    backgroundColor: '#050807',
    scrollEnabled: true,
    /* Doit rester false : sinon WKWebView bloque Supabase, Gemini et YouTube */
    limitsNavigationsToAppBoundDomains: false
  },

  plugins: {
    SplashScreen: {
      launchAutoHide: true,
      launchShowDuration: 500,
      backgroundColor: '#050807',
      showSpinner: false
    },
    StatusBar: {
      style: 'DARK',
      backgroundColor: '#050807',
      overlaysWebView: false
    }
  }
};

export default config;
